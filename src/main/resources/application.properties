# application.properties

# Datasource Configuration Local
# Aponta para um Postgres rodando na sua m�quina local
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=root
spring.datasource.password=Qwe12345

# JPA Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway Configuration
spring.flyway.enabled=true

# JWT Security Configuration
api.security.token.secret=segredo_super_secreto_da_equipe_mel_de_abelha_para_proteger_a_api_com_jwt
api.security.token.access-token-expiration-ms=1500000
api.security.token.refresh-token-expiration-ms=3000000

# Spring Cache Configuration
spring.cache.type=redis

# Redis Configuration (para rodar localmente)
spring.redis.host=localhost
spring.redis.port=6379
spring.cache.redis.time-to-live=10m